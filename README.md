//

Yesterday:

Successfully configured Events Service to run with a non-root user via CLI.
Ongoing investigation into running Events Service with a non-root user through the Enterprise Console.
Fine-tuned the server for Elastic Search as suggested.
Increased the maximum map count as recommended.
Confirmed no swap space is present on the system.
Verified that required ports are open in the firewall.

Today:
Continue investigation into running Events Service with a non-root user through the Enterprise Console.
Follow the next steps suggested by AppD Support to resolve all the issues related to Events Service.


